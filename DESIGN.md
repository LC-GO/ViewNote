# ViewNote 设计文档

> 版本 v0.1 · 2026-05-31 · Android（Capacitor + Web）
>
> 本文是产品与技术设计规划，既记录已实现的部分（标 ✅），也规划后续方向（标 🔜 / 💡）。
> 配套文档：使用与编译见 [README.md](README.md)。

## 目录

1. [产品定位与设计原则](#1-产品定位与设计原则)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [功能范围（MVP / v2 / v3）](#3-功能范围)
4. [信息架构与界面地图](#4-信息架构与界面地图)
5. [界面设计（线框图 + 交互）](#5-界面设计)
6. [UI 设计系统](#6-ui-设计系统)
7. [核心流程：三种打开方式](#7-核心流程三种打开方式)
8. [技术架构](#8-技术架构)
9. [渲染管线设计](#9-渲染管线设计)
10. [原生集成设计](#10-原生集成设计)
11. [数据模型](#11-数据模型)
12. [边界情况与错误处理](#12-边界情况与错误处理)
13. [非功能性需求](#13-非功能性需求)
14. [路线图](#14-路线图)

---

## 1. 产品定位与设计原则

**一句话定位**：一个轻量、快速、离线可用的 Android 文档查看器，专注于**舒服地阅读** `.md` 和 `.html` 文件。

**它不是什么**（划清边界，避免膨胀）：
- 不是编辑器（v1 只读；编辑是远期可选项）
- 不是笔记同步/云服务（纯本地，不联网）
- 不是浏览器（不做网页冲浪，只渲染本地文件）

**设计原则**：

| 原则 | 含义 | 体现 |
| --- | --- | --- |
| 📖 阅读优先 | 排版舒适、干净无干扰 | GitHub 风格排版、明暗主题、合理行距字号 |
| ⚡ 快速轻量 | 秒开、包小、无加载等待 | 纯前端渲染、无后端、APK 体积小 |
| 🚪 任意来源可入 | 文件从哪来都能打开 | 选择器 / 文件夹 / 打开方式 三入口 |
| 🔒 安全隔离 | 渲染不可信内容不出事 | Markdown 经 DOMPurify 净化；HTML 进沙箱 iframe |
| ✈️ 离线可用 | 不依赖网络 | 全部本地，断网照常用 |

---

## 2. 目标用户与使用场景

| 用户 | 场景 |
| --- | --- |
| 开发者 | 在手机上看项目 `README.md`、技术文档、导出的 API 文档 |
| 笔记用户 | 看从 Obsidian / Typora / 各类笔记 App 导出的 `.md` |
| 普通用户 | 收到 `.html` 报告、导出的网页、邮件附件，点开就看 |
| 临时查看 | 微信/邮件/网盘里收到文档，「分享 / 打开方式」直达 |

**核心用户旅程**：
> 在文件管理器里看到一个 `.md` → 长按「打开方式」→ 选 ViewNote → 立即看到排版好的内容 → 下次在 ViewNote「最近打开」里一键重看。

---

## 3. 功能范围

### ✅ MVP（已实现）
- 三种打开方式：系统文件选择器、内置文件夹浏览、打开方式/分享
- Markdown 渲染：标题、列表、表格、引用、链接、图片、**代码高亮**
- HTML 预览：沙箱 iframe 真实渲染 + 「预览 / 源码」切换
- 最近打开列表（本地持久化，可删除单项）
- 明暗主题切换（跟随系统 + 手动切换，记忆选择）
- Android 返回键、被「打开方式」拉起自动跳到查看页

### 🔜 v2（近期规划）
- **目录大纲 (TOC)**：长文档侧滑/弹出标题树，点击跳转
- **全文搜索**：当前文档内查找高亮
- **字号调节**：A- / A+ 调整阅读字号并记忆
- **收藏夹**：把常看文件/文件夹置顶
- **文件夹收藏 (SAF)**：用系统目录授权选一个笔记库目录，长期可浏览（解决 Android 13+ 浏览限制）
- **分享/导出**：把渲染结果导出为图片或 PDF

### 💡 v3（远期设想）
- 轻量编辑（Markdown 源码编辑 + 实时预览）
- Mermaid 图表、KaTeX 数学公式
- 图片相对路径解析（HTML/MD 引用同目录图片）
- 多标签 / 阅读历史 / 阅读进度记忆
- iOS 版本（同一套 Web 代码，加 iOS 平台）

---

## 4. 信息架构与界面地图

```
ViewNote
├── 主页 / 库 (Home)                ← 应用入口
│     ├─ [打开文件]  → 系统选择器 → 查看页
│     ├─ [浏览文件夹] → 文件夹浏览 → 查看页
│     └─ 最近打开列表 → 查看页
│
├── 查看页 (Viewer)                 ← 看内容
│     ├─ Markdown 模式
│     └─ HTML 模式 ↔ 源码模式
│
├── 文件夹浏览 (Browser)            ← 逐级浏览目录
│     └─ 点文件 → 查看页
│
└── 设置 (Settings) 🔜              ← v2：字号、默认主题、关于
```

外部入口（绕过主页，直达查看页）：
```
其他 App ──「打开方式 / 分享」──▶ 查看页（冷启动或热启动）
```

---

## 5. 界面设计

### 5.1 主页 / 库

```
┌──────────────────────────────┐
│ 📄 ViewNote              🌓   │  ← 顶栏：品牌 + 主题切换
├──────────────────────────────┤
│  ┌────────────────────────┐  │
│  │   📂  打开文件          │  │  ← 主操作（实心强调色）
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │   🗂️  浏览文件夹        │  │  ← 次操作（描边）
│  └────────────────────────┘  │
│                              │
│  最近打开                     │  ← 分组标题（弱化）
│  📝 demo.md              ✕    │  ← 列表项：图标+名称+删除
│  🌐 report.html          ✕    │
│  📝 notes.md             ✕    │
│                              │
└──────────────────────────────┘
```
**交互**：点列表项整行 → 打开；点 ✕ → 仅删除该条记录（不删文件）。空状态显示引导文案。

### 5.2 查看页 — Markdown

```
┌──────────────────────────────┐
│ ←   demo.md             🌓   │  ← 返回 + 文件名(超长省略) + 主题
├──────────────────────────────┤
│  # ViewNote 测试文档          │
│  这是一段 **正文**，行距宽松   │
│  ┌──────────────────────┐    │
│  │ code block (高亮深色) │    │  ← 代码块固定深色，明暗主题都清晰
│  └──────────────────────┘    │
│  | 表格 | 状态 |              │
│  | --- | --- |               │
└──────────────────────────────┘
```

### 5.3 查看页 — HTML（预览 ↔ 源码）

```
预览模式                          源码模式
┌──────────────────────────┐    ┌──────────────────────────┐
│ ←  report.html  〈〉  🌓 │    │ ←  report.html  👁️  🌓  │
├──────────────────────────┤    ├──────────────────────────┤
│ ╔══ sandbox iframe ════╗ │    │ <!doctype html>          │
│ ║  真实渲染的网页      ║ │ ⇄  │ <html>                   │
│ ║  (含自身 CSS/JS)     ║ │    │   <head> ... (高亮源码)  │
│ ╚══════════════════════╝ │    │ </html>                  │
└──────────────────────────┘    └──────────────────────────┘
        点 〈〉 切到源码 ──────▶  ◀────── 点 👁️ 切回预览
```

### 5.4 文件夹浏览

```
┌──────────────────────────────┐
│ ←   /Download                │  ← 返回(上一级/回主页) + 当前路径
├──────────────────────────────┤
│ 📁 docs                      │  ← 目录在前
│ 📁 notes                     │
│ 📝 todo.md                   │  ← 只列 .md/.html/.txt
│ 🌐 index.html                │
└──────────────────────────────┘
```
**交互**：点目录进入下一级；点文件打开；返回键/← 逐级回退，根目录再返回则回主页。只显示可读文档，过滤噪声。

---

## 6. UI 设计系统

### 6.1 配色（GitHub 风格，CSS 变量）

| Token | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#ffffff` | `#0d1117` | 页面背景 |
| `--bg-elev` | `#f5f6f8` | `#161b22` | 顶栏 / 卡片 / 表头 |
| `--text` | `#1f2328` | `#e6edf3` | 正文 |
| `--text-dim` | `#656d76` | `#8b949e` | 次要文字 / 分组标题 |
| `--border` | `#d0d7de` | `#30363d` | 分隔线 / 描边 |
| `--accent` | `#2f81f7` | `#2f81f7` | 主按钮 / 链接 |
| `--code-inline-bg` | `#eff1f3` | `#2a2f37` | 行内代码底色 |

> 代码**块**用固定深色主题（highlight.js atom-one-dark），无论明暗主题都保证可读，视觉上像「终端」一样统一。

### 6.2 字体与排版
- 字体栈：系统字体（`-apple-system / Segoe UI / PingFang SC / Microsoft YaHei / Roboto`）
- 正文 16px / 行距 1.7（阅读优先）
- 标题层级：H1 1.7em（带下划线）、H2 1.4em（带下划线）、H3 1.2em
- 长内容自动断词，图片 `max-width:100%` 自适应，表格横向滚动

### 6.3 组件清单
- **顶栏 TopBar**：固定高度 52px + 状态栏安全区，左操作/标题/右操作
- **主按钮 / 次按钮**：圆角 12px，主按钮实心强调色，次按钮描边
- **列表项 ListItem**：图标 + 主文本（省略号）+ 尾部操作
- **图标按钮 IconBtn**：40×40 点击区，按下态有底色反馈
- **Markdown 容器**：`.markdown-body` 统一排版样式

### 6.4 适配
- 移动端优先，`viewport-fit=cover` + `env(safe-area-inset-*)` 适配刘海/手势条
- `100dvh` 处理移动端动态视口高度
- 禁用双击缩放，整体作为 App 而非网页

### 6.5 主题系统（✅ 已集成到 APP · 预览页 `samples/theme-preview.html`）

**① 8 套阅读主题**（参考知名设计系统/编辑器；菜单切换；**代码块配色随主题明暗**）

浅色系（5）：

| 主题 | `--bg` | `--link` | 灵感来源 |
| --- | --- | --- | --- |
| GitHub 浅色（默认）| `#ffffff` | `#0969da` | GitHub |
| Notion | `#ffffff` | `#2383e2` | Notion |
| 护眼纸张 Sepia | `#f4ecd8` | `#9a5b27` | 阅读器 |
| Solarized Light | `#fdf6e3` | `#268bd2` | Solarized |
| Catppuccin Latte | `#eff1f5` | `#1e66f5` | Catppuccin |

深色系（3）：

| 主题 | `--bg` | `--link` | 灵感来源 |
| --- | --- | --- | --- |
| GitHub 深色 | `#0d1117` | `#2f81f7` | GitHub |
| OLED 纯黑 | `#000000` | `#4493f8` | 省电深夜 |
| One Dark | `#282c34` | `#61afef` | Atom |

> 早期曾备选 Gruvbox、Dracula、Nord、Tokyo Night、Catppuccin Mocha、Rosé Pine，但因彼此观感相近已精简掉。

每套完整 token 集（共 19 个变量）见 `samples/theme-preview.html`：`--bg / --bg-elev / --text / --text-dim / --heading / --border / --link / --quote-bar / --inline-code-bg / --code-bg / --code-fg / --tok-{key,str,com,fun,num} / --table-stripe / --note / --warn`。

**② 字体**：无衬线（系统栈）/ 衬线（Georgia + 思源宋体回退）可切换。⚠️ 中文衬线在部分安卓机会回退到系统字体。
**③ 字号**：A−/A+ 七档 `13/14/15/16/17/18/20/22`（默认 16）。
**④ 行距**：紧凑 1.5 / 标准 1.75 / 宽松 2.0（默认 1.75）。
**⑤ 特殊块**（一并美化）：GitHub 提示框 `> [!NOTE] / [!WARNING]`、任务清单 ☑️、表格斑马纹。
**⑥ 裸 HTML**：默认严格保持原样（不注入样式）；后续可加「无样式时套用基础排版」可选项。

**集成实现（✅ 已完成）**：CSS 变量主题层（8 套）；`.markdown-body` 全用变量；顶栏「Aa」打开底部「外观」面板，可选主题 / 字体（无衬线·衬线）/ 字号（A−A+ 七档）/ 行距（紧凑·标准·宽松）。持久化到 `viewnote.{theme,font,fontScale,lineHeight}`。**默认 `auto` 跟随系统**：系统浅色→GitHub 浅色，系统深色→GitHub 深色，并实时响应系统切换。代码高亮把 highlight.js 的 token class 映射到各主题的 `--tok-*` 变量（无需额外 hljs 主题文件），随主题明暗变化。

---

## 7. 核心流程：三种打开方式

### 7.1 系统文件选择器（最通用）
```
点[打开文件] → FilePicker.pickFiles({readData}) → 拿到 base64
           → UTF-8 解码 → detectType(按后缀/内容) → 存最近 → 查看页
```
- 原生用 `@capawesome/capacitor-file-picker`；浏览器降级为 `<input type=file>`（方便调试）。

### 7.2 内置文件夹浏览
```
点[浏览文件夹] → 申请存储权限 → Filesystem.readdir(ExternalStorage)
            → 过滤目录/文档 → 逐级导航 → 点文件 readFile → 查看页
```
- ⚠️ Android 13+ 分区存储限制：基础浏览仅在 12 及以下稳定；13+ 引导用其他两种方式，v2 用 SAF 目录授权补强。

### 7.3 打开方式 / 分享（直达，体验最好）
```
其他 App 点文件 → ACTION_VIEW / ACTION_SEND → MainActivity
           → FileOpenPlugin 用 ContentResolver 读出文本
           → 前端启动/resume 时 getOpenedFile() → 查看页
```
- **关键决策**：`ACTION_VIEW`（打开方式）和 `ACTION_SEND`（分享）由**自研** `FileOpenPlugin` 统一处理。常见的 `send-intent` 插件只支持 `SEND`，覆盖不了「打开方式」，故弃用、自研，直接返回文本内容（也规避了 `content://` 读取难题）。

---

## 8. 技术架构

### 8.1 分层
```
┌─────────────────────────────────────────────┐
│  UI 层      main.js   界面渲染/导航/状态机     │
├─────────────────────────────────────────────┤
│  渲染层     render.js  Markdown→HTML、代码高亮 │
├─────────────────────────────────────────────┤
│  数据/平台层 files.js  三种打开方式、最近文件   │
├─────────────────────────────────────────────┤
│  原生桥     Capacitor 插件                     │
│   ├ @capacitor/filesystem      读目录/文件     │
│   ├ @capawesome/file-picker    系统选择器      │
│   ├ @capacitor/app             返回键/生命周期 │
│   └ FileOpenPlugin (自研)      打开方式/分享   │
├─────────────────────────────────────────────┤
│  Android 原生壳 (WebView + MainActivity)       │
└─────────────────────────────────────────────┘
```

### 8.2 选型理由
- **Capacitor**：用 Web 技术写一次，原生壳承载；HTML 天然用 WebView 渲染，与「看文档」需求高度契合。
- **Vite**：极快的构建与开发服务器，输出到 `dist/` 供 Capacitor 打包。
- **无框架（原生 JS）**：界面简单（3 个屏），用原生 DOM + 模块化即可，避免引入 React/Vue 增加体积与复杂度。状态机用一个 `state` 对象 + `render*()` 函数。

### 8.3 状态管理
单一 `state` 对象：`{ view, current, htmlShowSource, browse }`。
视图切换 = 改 `state.view` + 调对应 `render函数()` 重绘 `#app`。简单、可预测，无需额外库。

---

## 9. 渲染管线设计

### 9.1 Markdown
```
原始文本 ─marked.parse()→ HTML字符串 ─DOMPurify.sanitize()→ 安全HTML
       → 注入 .markdown-body → 遍历 <pre><code> 调 hljs 高亮
```
- `marked`：GFM 开启（表格/删除线），`breaks:false` 贴近标准 Markdown。
- `DOMPurify`：净化掉脚本/危险属性，**防止恶意 Markdown 注入**。
- `highlight.js/lib/common`：只打包常见语言，控制体积。

### 9.2 HTML
```
原始文本 → <iframe srcdoc sandbox="allow-scripts allow-popups allow-forms allow-modals">
```
- 用沙箱 iframe **真实渲染**（含文件自身的 CSS/JS），所见即所得。
- 不加 `allow-same-origin` → iframe 与宿主**同源隔离**，无法访问 App 的存储/DOM，安全。
- 源码模式：把文本当代码用 hljs 高亮展示，便于查看结构。

### 9.3 类型判定
`detectType(name, content)`：先看后缀（`.md/.markdown/...` → markdown；`.html/.htm/...` → html），无后缀时看内容首部是否像 HTML 标签，默认按 Markdown。

---

## 10. 原生集成设计

### 10.1 AndroidManifest（intent-filter）
- `VIEW` 过滤器：`content`/`file` scheme + `text/html`、`text/plain`、`text/markdown` 等 MIME → 出现在「打开方式」。
- `SEND` 过滤器：`text/*` → 出现在分享菜单。

### 10.2 FileOpenPlugin（自研）
- `getOpenedFile()`：读当前 Intent，区分 VIEW/SEND，取 URI → `ContentResolver` 读文本 → 返回 `{name, text}`；读后清空 Intent 防重复。
- `MainActivity`：`onCreate` 注册插件；`onNewIntent` 更新 Intent（应对 `singleTask` 热启动）。

### 10.3 权限
- `INTERNET`（Capacitor 默认）
- `READ_EXTERNAL_STORAGE`（`maxSdkVersion=32`，文件夹浏览用）
- 🔜 v2 SAF：`ACTION_OPEN_DOCUMENT_TREE` 目录授权，免敏感权限即可长期访问指定目录。

### 10.4 生命周期
- 返回键：查看页/浏览页 → 上一级或主页；主页 → 退出 App。
- `resume`：重新检查是否有新的「打开方式」文件。

---

## 11. 数据模型

### 11.1 最近文件（localStorage: `viewnote.recents`）
```js
[{ name: string, type: 'markdown'|'html', text: string, ts: number }]  // 最多 20 条
```
- 直接存正文，保证离线可重开；写入超限时降级为只存元信息。
- 💡 v2 优化：正文超过阈值时只存路径/摘要，避免 localStorage 膨胀。

### 11.2 主题（localStorage: `viewnote.theme`）
`'light' | 'dark'`，缺省跟随系统 `prefers-color-scheme`。

### 11.3 🔜 设置（v2）
`{ fontScale, defaultTheme, favorites:[], savedFolders:[] }`

---

## 12. 边界情况与错误处理

| 情况 | 处理 |
| --- | --- |
| 超大文件 | v1 全量读入；v2 设上限/分块或提示 |
| 非 UTF-8 编码 | 目前按 UTF-8 解码；v2 可探测 GBK 等 |
| 无后缀文件 | 按内容猜测类型 |
| 损坏/畸形 Markdown | marked 容错渲染，不崩溃 |
| HTML 引用外部资源 | 沙箱内联网资源可加载，相对路径资源无法解析（v3 解决） |
| 权限被拒 | 文件夹浏览给出明确提示，引导改用其他入口 |
| 空文件夹 | 显示「无可显示文件」空状态 |
| content:// 读取失败 | 插件 try/catch，前端 toast 报错而非崩溃 |

---

## 13. 非功能性需求

- **性能**：主包 ~240KB（gzip ~81KB，含 marked/hljs/DOMPurify）；秒级冷启动。💡 可懒加载 hljs 进一步减小首包。
- **安全**：Markdown 净化 + HTML 沙箱隔离；不联网、无数据外传。
- **可访问性**：足够对比度、可调字号（v2）、语义化标签。
- **国际化**：界面中文为主；文档内容本身任意语言。
- **可维护性**：三个职责清晰的模块（UI/渲染/数据），原生改动集中在 `android/` 两个文件。

---

## 14. 路线图

```
v0.1 (现在) ✅  三入口打开 · MD/HTML 渲染 · 最近 · 8 套主题/字体/字号/行距
   │
   ▼
v0.2 🔜        真机编译验证 · 目录大纲 · 全文搜索 · 收藏夹
   │
   ▼
v0.3 🔜        SAF 文件夹收藏 · 收藏夹 · 导出图片/PDF
   │
   ▼
v1.0 💡        轻量编辑 · Mermaid/KaTeX · 相对路径图片
   │
   ▼
未来  💡        iOS 版（复用同一套 Web 代码）
```

**下一步（紧接当前进度）**：装好 JDK 21 + Android SDK，真机编译验证三种打开方式与渲染效果（对应任务清单第 7 项）。
